name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm

      - run: npm ci
      - run: npm run typecheck
      - run: npm run build
      - run: npm test
      # Dependency audit gate: fails on HIGH-or-worse vulnerabilities.
      # Re-run locally with: npm audit --omit=dev --audit-level=high
      # (after upgrades, re-run; fix with `npm audit fix` then re-run the audit).
      - run: npm audit --omit=dev --audit-level=high
